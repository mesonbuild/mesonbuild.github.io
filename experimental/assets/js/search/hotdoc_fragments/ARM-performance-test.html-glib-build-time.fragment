fragment_downloaded_cb({"url": "ARM-performance-test.html#glib-build-time", "fragment": "On desktop machines Ninja based build systems are faster than Make based ones. On this platform the difference grows to The difference is probably caused by Make s inefficient disk access patterns. Ninja is better at keeping both cores running all the time which yields impressive performance improvements. \n\n"});