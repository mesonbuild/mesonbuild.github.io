fragment_downloaded_cb({"url": "Getting-meson.html#installer-step-3", "fragment": "With this you will have python and pip in PATH and you can install Meson with pip. You will also be able to directly run meson in any shell on Windows instead of having to run py with the full path to the meson.py script. \n"});