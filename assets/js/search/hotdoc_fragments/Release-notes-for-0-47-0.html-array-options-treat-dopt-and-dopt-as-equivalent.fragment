fragment_downloaded_cb({"url":"Release-notes-for-0-47-0.html#array-options-treat-dopt-and-dopt-as-equivalent","fragment":"Array options treat -Dopt= and -Dopt=[] as equivalent\nPrior to this change passing -Dopt= to an array opt would be\ninterpreted as [''] (an array with an empty string), now -Dopt= is\nthe same as -Dopt=[], an empty list.\n"});