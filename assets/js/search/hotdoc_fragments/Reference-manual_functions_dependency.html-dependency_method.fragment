fragment_downloaded_cb({"url":"Reference-manual_functions_dependency.html#dependency_method","fragment":"Defines the way the dependency is detected, the default is\nauto but can be overridden to be e.g. qmake for Qt development,\nand different dependencies support different values\nfor this (though auto will work on all of them)\n(since 0.40.0)\ndefault =\n  'auto'\n"});