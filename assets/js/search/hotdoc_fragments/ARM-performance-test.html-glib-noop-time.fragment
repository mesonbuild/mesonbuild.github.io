fragment_downloaded_cb({"fragment": "Next we measured the empty build case. That is how long does it take for the build system to detect that no changes need to be made. This is one of the most important metrics of build systems because it places a hard limit on how fast you can iterate on your code. Autotools takes seconds to determine that no work needs to be done. Meson or rather Ninja takes just one quarter of a second. \n\n", "url": "ARM-performance-test.html#glib-noop-time"});