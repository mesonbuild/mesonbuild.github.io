fragment_downloaded_cb({"url":"Release-notes-for-0-58-0.html#multiple-append-and-prepend-in-environment-object","fragment":"Multiple append() and prepend() in environment() object\nappend() and prepend() methods can now be called multiple times\non the same varname. Earlier Meson versions would warn and only the last\noperation was taking effect.\n"});