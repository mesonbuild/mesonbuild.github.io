fragment_downloaded_cb({"url":"Reference-manual_functions_test.html#test_depends","fragment":"specifies that this test depends on the specified\ntarget(s), even though it does not take any of them as a command\nline argument. This is meant for cases where test finds those\ntargets internally, e.g. plugins or globbing. Those targets are built\nbefore test is executed even if they have build_by_default : false.\n(since 0.46.0)\n"});